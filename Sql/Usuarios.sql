CREATE TABLE usuarios (
    codigoUsuario VARCHAR(20) PRIMARY KEY,
    nombre VARCHAR(50),
    apellido VARCHAR(50),
    departamento VARCHAR(100),
    correo VARCHAR(90),
    telefono VARCHAR(20)
);
